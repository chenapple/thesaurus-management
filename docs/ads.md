智能广告 (Smart Ads)

     新标签页名称：智能广告
     基于多智能体架构，实现亚马逊广告数据分析与优化建议自动生成

     ---
     一、功能概述

     核心目标

     用户上传亚马逊广告报表 → 多智能体协同分析 → 自动生成优化建议

     用户操作流程

     1. 从亚马逊广告后台下载"搜索词报告"
     2. 拖拽上传到 App
     3. 点击"开始分析"
     4. 查看优化建议，导出执行

     系统处理流程

     ┌─────────────────────────────────────────────────────────────────┐
     │              用户拖拽上传 Excel 报表                             │
     └─────────────────────────────────────────────────────────────────┘
                                   ↓
     ┌─────────────────────────────────────────────────────────────────┐
     │                    数据解析与导入                                │
     │                                                                 │
     │  解析 Excel → 字段映射 → 存入数据库 → 计算统计指标               │
     └─────────────────────────────────────────────────────────────────┘
                                   ↓
                   ┌───────────────┼───────────────┐
                   ↓               ↓               ↓
             ┌─────────┐     ┌─────────┐     ┌─────────┐
             │ 搜索词   │     │  ACOS   │     │  竞价   │
             │ 分析师   │     │  专家   │     │ 策略师  │
             │   🤖    │     │   🤖    │     │   🤖    │
             └────┬────┘     └────┬────┘     └────┬────┘
                   │               │               │  (并行执行)
                   └───────────────┼───────────────┘
                                   ↓
                         ┌─────────────────┐
                         │   建议整合器     │
                         │      🤖        │
                         └────────┬────────┘
                                   ↓
                   ┌───────────────┼───────────────┐
                   ↓               ↓               ↓
             ┌─────────┐     ┌─────────┐     ┌─────────┐
             │ 否定词   │     │ 竞价调整 │     │ 新词    │
             │ 建议     │     │ 建议     │     │ 机会    │
             └─────────┘     └─────────┘     └─────────┘

     ---
     二、数据结构设计

     2.1 数据库表

     -- 广告项目表
     CREATE TABLE ad_projects (
         id INTEGER PRIMARY KEY AUTOINCREMENT,
         product_id INTEGER NOT NULL,
         name TEXT NOT NULL,
         marketplace TEXT DEFAULT 'US',
         target_acos REAL DEFAULT 30.0,  -- 目标 ACOS
         created_at TEXT DEFAULT CURRENT_TIMESTAMP,
         updated_at TEXT,
         FOREIGN KEY (product_id) REFERENCES products(id)
     );

     -- 搜索词数据表（从报表导入）
     CREATE TABLE ad_search_terms (
         id INTEGER PRIMARY KEY AUTOINCREMENT,
         project_id INTEGER NOT NULL,
         campaign_name TEXT,           -- 广告活动名称
         ad_group_name TEXT,           -- 广告组名称
         targeting TEXT,               -- 投放词/ASIN
         match_type TEXT,              -- 匹配类型: broad/phrase/exact
         customer_search_term TEXT,    -- 客户搜索词
         impressions INTEGER DEFAULT 0,
         clicks INTEGER DEFAULT 0,
         ctr REAL DEFAULT 0,           -- 点击率
         spend REAL DEFAULT 0,         -- 花费
         sales REAL DEFAULT 0,         -- 销售额
         orders INTEGER DEFAULT 0,     -- 订单数
         acos REAL DEFAULT 0,          -- ACOS
         roas REAL DEFAULT 0,          -- ROAS
         conversion_rate REAL DEFAULT 0, -- 转化率
         cpc REAL DEFAULT 0,           -- 单次点击成本
         report_date TEXT,             -- 报告日期
         imported_at TEXT DEFAULT CURRENT_TIMESTAMP,
         FOREIGN KEY (project_id) REFERENCES ad_projects(id)
     );

     -- 分析结果表
     CREATE TABLE ad_analysis_results (
         id INTEGER PRIMARY KEY AUTOINCREMENT,
         project_id INTEGER NOT NULL,
         analysis_type TEXT NOT NULL,  -- 'negative_words' | 'bid_adjustments' | 'keyword_opportunities'
         result_json TEXT NOT NULL,
         ai_provider TEXT,
         ai_model TEXT,
         created_at TEXT DEFAULT CURRENT_TIMESTAMP,
         FOREIGN KEY (project_id) REFERENCES ad_projects(id)
     );

     -- 创建索引
     CREATE INDEX idx_ad_search_terms_project ON ad_search_terms(project_id);
     CREATE INDEX idx_ad_search_terms_acos ON ad_search_terms(acos);
     CREATE INDEX idx_ad_search_terms_spend ON ad_search_terms(spend);

     2.2 TypeScript 类型定义

     // src/types.ts 新增

     // 广告项目
     export interface AdProject {
       id: number;
       product_id: number;
       name: string;
       marketplace: string;
       target_acos: number;
       created_at: string;
       updated_at?: string;
     }

     // 搜索词数据
     export interface AdSearchTerm {
       id: number;
       project_id: number;
       campaign_name: string;
       ad_group_name: string;
       targeting: string;
       match_type: 'broad' | 'phrase' | 'exact' | 'auto';
       customer_search_term: string;
       impressions: number;
       clicks: number;
       ctr: number;
       spend: number;
       sales: number;
       orders: number;
       acos: number;
       roas: number;
       conversion_rate: number;
       cpc: number;
       report_date: string;
     }

     // 否定词建议
     export interface NegativeWordSuggestion {
       search_term: string;
       reason: string;           // 建议原因
       risk_level: 'high' | 'medium' | 'low';
       spend_wasted: number;     // 浪费的花费
       match_type_suggestion: 'exact' | 'phrase';  // 建议的否定匹配类型
       campaigns_affected: string[];  // 影响的广告活动
     }

     // 竞价调整建议
     export interface BidAdjustment {
       targeting: string;        // 投放词
       campaign_name: string;
       current_performance: {
         acos: number;
         conversion_rate: number;
         impressions: number;
         clicks: number;
       };
       suggestion: 'increase' | 'decrease' | 'pause' | 'maintain';
       adjustment_percent: number;  // 建议调整幅度
       reason: string;
       priority: 'high' | 'medium' | 'low';
     }

     // 关键词机会
     export interface KeywordOpportunity {
       search_term: string;
       performance: {
         orders: number;
         conversion_rate: number;
         acos: number;
       };
       suggestion: string;       // 建议操作
       match_type: string;       // 建议匹配类型
       estimated_potential: string;  // 预估潜力
     }

     // 分析结果汇总
     export interface AdAnalysisResult {
       negative_words: NegativeWordSuggestion[];
       bid_adjustments: BidAdjustment[];
       keyword_opportunities: KeywordOpportunity[];
       summary: {
         total_spend_analyzed: number;
         potential_savings: number;
         optimization_score: number;
         key_insights: string[];
       };
     }

     ---
     三、多智能体设计

     3.1 智能体架构

     ┌─────────────────────────────────────────────────────────────┐
     │                      协调器 (Coordinator)                    │
     │  - 接收用户请求                                              │
     │  - 分解任务，分配给各智能体                                   │
     │  - 监控执行进度                                              │
     │  - 整合最终结果                                              │
     └─────────────────────────────────────────────────────────────┘
                         │
         ┌───────────────┼───────────────┐
         ↓               ↓               ↓
     ┌─────────┐   ┌─────────┐   ┌─────────┐
     │ 搜索词   │   │ ACOS    │   │ 竞价    │
     │ 分析师   │   │ 专家    │   │ 策略师  │
     │         │   │         │   │         │
     │ 识别:    │   │ 分析:   │   │ 建议:   │
     │ - 无效词 │   │ - 效率  │   │ - 加价  │
     │ - 高转化 │   │ - 趋势  │   │ - 降价  │
     │ - 低转化 │   │ - 异常  │   │ - 暂停  │
     └────┬────┘   └────┬────┘   └────┬────┘
          │             │             │
          └─────────────┼─────────────┘
                        ↓
               ┌───────────────┐
               │  建议整合器    │
               │               │
               │ 生成:         │
               │ - 否定词列表   │
               │ - 竞价调整表   │
               │ - 新词建议     │
               │ - 执行优先级   │
               └───────────────┘

     3.2 智能体 Prompt 设计

     // src/ad-prompts.ts

     // 搜索词分析师 Prompt
     export function buildSearchTermAnalystPrompt(
       searchTerms: AdSearchTerm[],
       targetAcos: number
     ): string {
       return `你是一位资深的亚马逊 PPC 搜索词分析师。

     ## 任务
     分析以下搜索词数据，识别需要否定的无效词和高潜力词。

     ## 目标 ACOS
     ${targetAcos}%

     ## 搜索词数据
     ${JSON.stringify(searchTerms.slice(0, 200), null, 2)}

     ## 分析维度
     1. **无效搜索词识别**
        - 高花费零转化（spend > 平均值 && orders = 0）
        - 极低转化率（conversion_rate < 1% && clicks > 10）
        - 不相关词（通过语义判断）

     2. **高潜力词识别**
        - 高转化低 ACOS（acos < 目标 && orders >= 2）
        - 低展示高转化（需要加大投放）

     ## 输出格式（JSON）
     {
       "negative_candidates": [
         {
           "search_term": "xxx",
           "reason": "高花费零转化，花费$XX",
           "risk_level": "high",
           "spend_wasted": 12.5,
           "match_type_suggestion": "exact"
         }
       ],
       "high_potential": [
         {
           "search_term": "xxx",
           "performance": { "orders": 5, "acos": 15 },
           "suggestion": "建议精准投放"
         }
       ],
       "insights": ["洞察1", "洞察2"]
     }`;
     }

     // ACOS 专家 Prompt
     export function buildAcosExpertPrompt(
       searchTerms: AdSearchTerm[],
       targetAcos: number
     ): string {
       return `你是一位亚马逊广告 ACOS 优化专家。

     ## 任务
     分析广告效率，识别 ACOS 异常的投放词，并提供优化建议。

     ## 目标 ACOS: ${targetAcos}%

     ## 数据摘要
     - 总花费: $${searchTerms.reduce((s, t) => s + t.spend, 0).toFixed(2)}
     - 总销售: $${searchTerms.reduce((s, t) => s + t.sales, 0).toFixed(2)}
     - 平均 ACOS: ${(searchTerms.reduce((s, t) => s + t.acos, 0) / searchTerms.length).toFixed(1)}%

     ## 按 ACOS 分组的搜索词
     ### 超高 ACOS (>100%)
     ${JSON.stringify(searchTerms.filter(t => t.acos > 100).slice(0, 50), null, 2)}

     ### 高 ACOS (${targetAcos * 1.5}% - 100%)
     ${JSON.stringify(searchTerms.filter(t => t.acos > targetAcos * 1.5 && t.acos <= 100).slice(0, 50), null, 
     2)}

     ### 达标 ACOS (<${targetAcos}%)
     ${JSON.stringify(searchTerms.filter(t => t.acos > 0 && t.acos <= targetAcos).slice(0, 50), null, 2)}

     ## 输出格式（JSON）
     {
       "efficiency_analysis": {
         "profitable_keywords_count": 10,
         "losing_keywords_count": 25,
         "break_even_keywords_count": 15
       },
       "acos_distribution": {
         "excellent": { "count": 5, "total_spend": 100 },
         "good": { "count": 10, "total_spend": 200 },
         "poor": { "count": 20, "total_spend": 500 }
       },
       "optimization_priorities": [
         {
           "targeting": "xxx",
           "current_acos": 85,
           "issue": "ACOS 过高但有转化",
           "suggestion": "降低竞价 20%"
         }
       ],
       "insights": ["洞察1", "洞察2"]
     }`;
     }

     // 竞价策略师 Prompt
     export function buildBidStrategistPrompt(
       searchTerms: AdSearchTerm[],
       targetAcos: number
     ): string {
       return `你是一位亚马逊广告竞价策略专家。

     ## 任务
     基于搜索词表现数据，生成具体的竞价调整建议。

     ## 目标 ACOS: ${targetAcos}%

     ## 投放词表现数据（按花费排序前 100）
     ${JSON.stringify(
       searchTerms
         .sort((a, b) => b.spend - a.spend)
         .slice(0, 100)
         .map(t => ({
           targeting: t.targeting,
           campaign: t.campaign_name,
           match_type: t.match_type,
           spend: t.spend,
           sales: t.sales,
           acos: t.acos,
           orders: t.orders,
           cpc: t.cpc,
           impressions: t.impressions
         })),
       null, 2
     )}

     ## 竞价调整原则
     1. **加价条件**: ACOS < 目标 && 转化率 > 5% && 展示量偏低
     2. **降价条件**: ACOS > 目标 * 1.5 && 有转化
     3. **暂停条件**: ACOS > 100% || (花费 > $20 && 零转化)
     4. **维持条件**: ACOS 接近目标 ± 20%

     ## 输出格式（JSON）
     {
       "bid_adjustments": [
         {
           "targeting": "xxx",
           "campaign_name": "xxx",
           "current_performance": {
             "acos": 45,
             "conversion_rate": 8,
             "impressions": 1000,
             "clicks": 50
           },
           "suggestion": "decrease",
           "adjustment_percent": -20,
           "reason": "ACOS 超出目标 50%，建议降价观察",
           "priority": "high"
         }
       ],
       "summary": {
         "increase_count": 5,
         "decrease_count": 15,
         "pause_count": 8,
         "maintain_count": 72,
         "estimated_savings": 150.00
       }
     }`;
     }

     // 建议整合器 Prompt
     export function buildSuggestionIntegratorPrompt(
       searchTermAnalysis: any,
       acosAnalysis: any,
       bidStrategy: any,
       targetAcos: number
     ): string {
       return `你是广告优化建议整合专家。

     ## 任务
     整合三位专家的分析结果，生成最终的优化建议报告。

     ## 搜索词分析师结果
     ${JSON.stringify(searchTermAnalysis, null, 2)}

     ## ACOS 专家结果
     ${JSON.stringify(acosAnalysis, null, 2)}

     ## 竞价策略师结果
     ${JSON.stringify(bidStrategy, null, 2)}

     ## 输出要求
     1. 合并去重否定词建议
     2. 整合竞价调整建议并按优先级排序
     3. 提取关键词机会
     4. 生成执行摘要

     ## 输出格式（JSON）
     {
       "negative_words": [
         {
           "search_term": "xxx",
           "reason": "综合原因",
           "risk_level": "high",
           "spend_wasted": 25.00,
           "match_type_suggestion": "exact",
           "campaigns_affected": ["Campaign A", "Campaign B"]
         }
       ],
       "bid_adjustments": [
         {
           "targeting": "xxx",
           "campaign_name": "xxx",
           "suggestion": "decrease",
           "adjustment_percent": -20,
           "reason": "xxx",
           "priority": "high"
         }
       ],
       "keyword_opportunities": [
         {
           "search_term": "xxx",
           "performance": { "orders": 5, "acos": 12 },
           "suggestion": "添加为精准匹配关键词",
           "match_type": "exact",
           "estimated_potential": "高"
         }
       ],
       "summary": {
         "total_spend_analyzed": 5000.00,
         "potential_savings": 800.00,
         "optimization_score": 72,
         "key_insights": [
           "发现 25 个高风险否定词，预计节省 $XXX",
           "15 个关键词需要降价，当前 ACOS 超标",
           "发现 8 个高潜力新词机会"
         ]
       }
     }`;
     }

     ---
     四、前端组件设计

     4.1 文件结构

     src/components/
     ├── AdOptimizerTab.vue              # 主组件
     ├── ad-optimizer/
     │   ├── AdProjectList.vue           # 项目列表
     │   ├── AdDataImport.vue            # 数据导入组件
     │   ├── AdAnalysisCanvas.vue        # 可视化分析画布
     │   ├── AdAnalysisResults.vue       # 结果展示
     │   └── nodes/
     │       ├── DataSourceNode.vue      # 数据源节点
     │       ├── AgentNode.vue           # 智能体节点
     │       └── ResultNode.vue          # 结果节点

     4.2 主组件结构 (AdOptimizerTab.vue)

     <template>
       <div class="ad-optimizer-tab">
         <!-- 项目列表视图 -->
         <div v-if="viewMode === 'list'" class="project-list-view">
           <AdProjectList
             :projects="projects"
             @select="enterProject"
             @create="showCreateDialog = true"
           />
         </div>

         <!-- 项目详情视图 -->
         <div v-else class="project-detail-view">
           <div class="detail-header">
             <el-button @click="viewMode = 'list'">返回列表</el-button>
             <h3>{{ currentProject?.name }}</h3>
             <span>目标 ACOS: {{ currentProject?.target_acos }}%</span>
           </div>

           <!-- 数据导入区 -->
           <AdDataImport
             v-if="!hasData"
             :project-id="currentProject?.id"
             @imported="loadSearchTerms"
           />

           <!-- 数据概览 -->
           <div v-if="hasData" class="data-overview">
             <el-row :gutter="16">
               <el-col :span="6">
                 <div class="stat-card">
                   <div class="stat-value">${{ totalSpend.toFixed(2) }}</div>
                   <div class="stat-label">总花费</div>
                 </div>
               </el-col>
               <el-col :span="6">
                 <div class="stat-card">
                   <div class="stat-value">${{ totalSales.toFixed(2) }}</div>
                   <div class="stat-label">总销售</div>
                 </div>
               </el-col>
               <el-col :span="6">
                 <div class="stat-card">
                   <div class="stat-value">{{ averageAcos.toFixed(1) }}%</div>
                   <div class="stat-label">平均 ACOS</div>
                 </div>
               </el-col>
               <el-col :span="6">
                 <div class="stat-card">
                   <div class="stat-value">{{ searchTerms.length }}</div>
                   <div class="stat-label">搜索词数量</div>
                 </div>
               </el-col>
             </el-row>
           </div>

           <!-- AI 分析区 -->
           <div v-if="hasData" class="ai-analysis-section">
             <div class="section-header">
               <span>AI 多智能体分析</span>
               <div class="controls">
                 <el-select v-model="selectedProvider" size="small">
                   <el-option label="DeepSeek" value="deepseek" />
                   <el-option label="OpenAI" value="openai" />
                 </el-select>
                 <el-button type="primary" @click="startAnalysis" :loading="isAnalyzing">
                   {{ analysisResult ? '重新分析' : '开始分析' }}
                 </el-button>
               </div>
             </div>

             <!-- 可视化画布（分析进行中） -->
             <AdAnalysisCanvas
               v-if="isAnalyzing"
               :status="analysisStatus"
               :streaming-content="streamingContent"
             />

             <!-- 分析结果 -->
             <AdAnalysisResults
               v-if="analysisResult"
               :result="analysisResult"
               @export="exportResults"
             />
           </div>
         </div>
       </div>
     </template>

     4.3 可视化画布节点布局

             ┌─────────────────┐
             │   广告报表数据   │
             │    (已导入)     │
             └────────┬────────┘
                      │
          ┌───────────┼───────────┐
          ↓           ↓           ↓
     ┌─────────┐ ┌─────────┐ ┌─────────┐
     │ 搜索词   │ │  ACOS   │ │  竞价   │
     │ 分析师   │ │  专家   │ │ 策略师  │
     │ ⏳ 执行中│ │ ⏳ 执行中│ │ ⏳ 执行中│  ← 并行执行
     └────┬────┘ └────┬────┘ └────┬────┘
          │           │           │
          └───────────┼───────────┘
                      ↓
             ┌─────────────────┐
             │   建议整合器     │
             │    ⏸ 等待中     │
             └────────┬────────┘
                      │
          ┌───────────┼───────────┐
          ↓           ↓           ↓
     ┌─────────┐ ┌─────────┐ ┌─────────┐
     │ 否定词   │ │ 竞价调整 │ │ 新词    │
     │ 建议     │ │ 建议     │ │ 机会    │
     └─────────┘ └─────────┘ └─────────┘

     ---
     五、实现步骤

     阶段 1: 基础架构

     1. 数据库表创建
       - 修改 src-tauri/src/db.rs，添加 ad_projects、ad_search_terms、ad_analysis_results 表
     2. 类型定义
       - 修改 src/types.ts，添加广告相关类型
     3. API 层
       - 修改 src/api.ts，添加广告相关 API 调用
       - 修改 src-tauri/src/lib.rs，添加 Tauri 命令

     阶段 2: 数据导入

     1. Excel 解析
       - 创建 src/utils/ad-parser.ts，解析亚马逊搜索词报告格式
       - 支持中英文列名自动映射
     2. 导入组件
       - 创建 AdDataImport.vue，支持拖拽上传
       - 数据预览和确认
       - 导入进度显示

     阶段 3: 多智能体分析

     1. Prompt 模板
       - 创建 src/ad-prompts.ts，定义各智能体 Prompt
     2. 分析流程
       - 搜索词分析师、ACOS 专家、竞价策略师并行执行
       - 建议整合器汇总结果

     阶段 4: 可视化与结果展示

     1. 画布组件
       - 创建 AdAnalysisCanvas.vue，复用 Vue Flow
       - 智能体节点 + 连线动画
     2. 结果展示
       - 创建 AdAnalysisResults.vue
       - 否定词建议 Tab
       - 竞价调整 Tab
       - 新词机会 Tab
     3. 导出功能
       - 导出否定词 Excel（可直接用于亚马逊批量上传）
       - 导出竞价调整建议 Excel

     ---
     六、关键文件清单
     ┌───────────────────────────────────────────────────┬──────┬──────────────────────────┐
     │                       文件                        │ 类型 │           说明           │
     ├───────────────────────────────────────────────────┼──────┼──────────────────────────┤
     │ src-tauri/src/db.rs                               │ 修改 │ 新增广告相关表及 CRUD    │
     ├───────────────────────────────────────────────────┼──────┼──────────────────────────┤
     │ src-tauri/src/lib.rs                              │ 修改 │ 新增 Tauri 命令          │
     ├───────────────────────────────────────────────────┼──────┼──────────────────────────┤
     │ src/types.ts                                      │ 修改 │ 新增广告相关类型定义     │
     ├───────────────────────────────────────────────────┼──────┼──────────────────────────┤
     │ src/api.ts                                        │ 修改 │ 新增广告 API 函数        │
     ├───────────────────────────────────────────────────┼──────┼──────────────────────────┤
     │ src/ad-prompts.ts                                 │ 新建 │ 智能体 Prompt 模板       │
     ├───────────────────────────────────────────────────┼──────┼──────────────────────────┤
     │ src/utils/ad-parser.ts                            │ 新建 │ 亚马逊报表 Excel 解析    │
     ├───────────────────────────────────────────────────┼──────┼──────────────────────────┤
     │ src/App.vue                                       │ 修改 │ 新增 AdOptimizerTab 入口 │
     ├───────────────────────────────────────────────────┼──────┼──────────────────────────┤
     │ src/components/AdOptimizerTab.vue                 │ 新建 │ 主组件                   │
     ├───────────────────────────────────────────────────┼──────┼──────────────────────────┤
     │ src/components/ad-optimizer/AdProjectList.vue     │ 新建 │ 项目列表                 │
     ├───────────────────────────────────────────────────┼──────┼──────────────────────────┤
     │ src/components/ad-optimizer/AdDataImport.vue      │ 新建 │ 数据导入（拖拽上传）     │
     ├───────────────────────────────────────────────────┼──────┼──────────────────────────┤
     │ src/components/ad-optimizer/AdAnalysisCanvas.vue  │ 新建 │ 分析可视化画布           │
     ├───────────────────────────────────────────────────┼──────┼──────────────────────────┤
     │ src/components/ad-optimizer/AdAnalysisResults.vue │ 新建 │ 结果展示                 │
     └───────────────────────────────────────────────────┴──────┴──────────────────────────┘
     ---
     七、验证方法

     1. 导入测试
       - 从亚马逊广告后台下载搜索词报告
       - 拖拽上传到 App
       - 验证数据解析正确、统计指标准确
     2. 分析测试
       - 运行多智能体分析
       - 验证否定词建议合理性
       - 验证竞价调整建议合理性
     3. 导出测试
       - 导出否定词 Excel
       - 在亚马逊广告后台批量导入验证格式正确

     ---
     八、预期效果

     分析报告示例

     ┌─────────────────────────────────────────────────────────┐
     │              广告优化分析报告                            │
     ├─────────────────────────────────────────────────────────┤
     │  分析数据：1,234 个搜索词 | 总花费 $5,432 | 总销售 $12,890 │
     │  优化评分：72/100                                        │
     ├─────────────────────────────────────────────────────────┤
     │                                                         │
     │  📌 关键发现                                             │
     │  • 发现 45 个高风险否定词，预计节省 $890/月              │
     │  • 23 个关键词 ACOS 超标，建议降价 15-30%                │
     │  • 发现 12 个高潜力新词，建议精准投放                    │
     │                                                         │
     ├─────────────────────────────────────────────────────────┤
     │  🚫 否定词建议 (45个)          [导出 Excel]              │
     │  ┌──────────────────┬────────┬─────────┬───────────┐    │
     │  │ 搜索词           │ 风险   │ 浪费花费 │ 建议类型  │    │
     │  ├──────────────────┼────────┼─────────┼───────────┤    │
     │  │ free shipping    │ 🔴 高  │ $45.20  │ 精准否定  │    │
     │  │ cheap xxx        │ 🔴 高  │ $32.10  │ 词组否定  │    │
     │  │ ...              │        │         │           │    │
     │  └──────────────────┴────────┴─────────┴───────────┘    │
     │                                                         │
     ├─────────────────────────────────────────────────────────┤
     │  💰 竞价调整建议 (38个)        [导出 Excel]              │
     │  ┌──────────────────┬────────┬────────┬────────────┐    │
     │  │ 投放词           │ 当前ACOS│ 建议   │ 调整幅度   │    │
     │  ├──────────────────┼────────┼────────┼────────────┤    │
     │  │ wireless earbuds │ 85%    │ 降价   │ -25%       │    │
     │  │ bluetooth headset│ 12%    │ 加价   │ +15%       │    │
     │  │ ...              │        │        │            │    │
     │  └──────────────────┴────────┴────────┴────────────┘    │
     │                                                         │
     └─────────────────────────────────────────────────────────┘

